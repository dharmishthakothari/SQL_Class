create database 14April 

use 14april

create table student(stud_name varchar(20),address varchar(20),m_no int(10) ,age int
(3),email varchar(20))



2nd May

delimiter $$
create function calculate_total(qty int,price int)
returns int
deterministic
begin 
declare total int;
set total:=qty*price;
return total;
end$$
delimiter ;



DELIMITER // 
create function square (num int)
returns int 
DETERMINISTIC
begin
declare ans int;
set ans:=num*num;
return ans;


create view view_event as select title from campus_events;

select * from view_event
end//
DELIMITER ;

5th May

create view view_dept_emp as 
select emp_id,emp_name,salary,dept_name from employee,department 
where employee.dept_id=department.dept_id

select dept_name,emp_name from view_dept_emp where dept_name='HR'

DDL     --- create , alter , drop 
DML   ---- insert , update , delete 
DCL   --- grant and revoke
DQL   --- select 
TCL --- rollback , savepoint ,commit




